apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-example.fullname" . }}
  labels:
    {{- include "api-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: api
      protocol: TCP
      name: api
  selector:
    {{- include "api-example.selectorLabels" . | nindent 4 }}
